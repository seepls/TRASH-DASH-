backend as a service (baas) and serverless .

SPA V/s MPA 

next.js : framwork that is making the workflow of MPA easier, giving the same SPA Experience with rendering html on the server.

FOR SPA Frameworks : Angular , React

Angular framework uses TypeScript 
React library : JS 


Iconic or NativeScript : Hybrid App 


PWA (Progressive Web App )

Web Components are another tool you should consider if you want to develop front end apps. 
they are a set of web platform APIs that allow you to create new custom, reusable, encapsulated
HTML tags to use in web pages and web apps.

. A Hybrid framework is a development tool which allows developers to use a single code base and 
release to both, App and Play stores

GraphQL is an open-source data query and manipulation language for APIs,
and a runtime for fulfilling queries with existing data.
